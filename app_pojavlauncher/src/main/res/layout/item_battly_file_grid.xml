<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="116dp"
    android:layout_margin="5dp"
    android:background="@drawable/bg_battly_settings_item"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="10dp">

        <ImageView
            android:id="@+id/battly_file_icon"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:padding="7dp"
            app:tint="#FFFFFFFF" />

        <TextView
            android:id="@+id/battly_file_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:ellipsize="middle"
            android:fontFamily="@font/noto_sans_bold"
            android:gravity="center"
            android:singleLine="true"
            android:textColor="#FFFFFFFF"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/battly_file_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="#AFC2CE"
            android:textSize="9sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/battly_file_delete"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:background="@drawable/bg_battly_launcher_chip"
        android:contentDescription="@string/global_delete"
        android:padding="0dp"
        android:scaleType="center"
        android:src="@drawable/ic_delete_compact"
        android:tint="#FFFF9A9A" />
</FrameLayout>
