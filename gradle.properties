# required for building jre_lwjgl3glfw with Java 8 while using plugins in app_pojavlauncher that require Java 11
org.gradle.configureondemand=true

android.useAndroidX=true
android.bundle.language.enableSplit=false
# Increase Gradle daemon RAM allocation
org.gradle.jvmargs=-Xmx4096M
android.nonTransitiveRClass=false
android.nonFinalResIds=false
android.javaCompile.suppressSourceTargetDeprecationWarning=true
android.r8.optimizedResourceShrinking=true

# BattlyWorlds is excluded from runtime and the merged manifest by default.
# Re-enable it with: ./gradlew assembleDebug -PbattlyWorldsEnabled=true
battlyWorldsEnabled=false
