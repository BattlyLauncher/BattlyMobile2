diff --git a/src/easytier/linkage_impl.rs b/src/easytier/linkage_impl.rs
index 84f77f9..594d566 100644
--- a/src/easytier/linkage_impl.rs
+++ b/src/easytier/linkage_impl.rs
@@ -31,6 +31,7 @@ struct EasyTierHolder {
 }
 
 pub fn create(args: Vec<Argument>) -> EasyTier {
+    let no_tun = args.iter().any(|argument| matches!(argument, Argument::NoTun));
     let table = UnsafeCell::new(Table::new());
     let acquire_table = || {
         unsafe {
@@ -171,9 +172,9 @@ pub fn create(args: Vec<Argument>) -> EasyTier {
         }
         return EasyTier(None);
     };
-    let tun_fd = instance.launcher.as_ref().unwrap().data.tun_fd.clone();
-
-    runtime.spawn(async move {
+    if !no_tun {
+        let tun_fd = instance.launcher.as_ref().unwrap().data.tun_fd.clone();
+        runtime.spawn(async move {
         let mut p_address = None;
         let mut p_proxy_cidrs = vec![];
 
@@ -210,7 +211,8 @@ pub fn create(args: Vec<Argument>) -> EasyTier {
             p_proxy_cidrs = proxy_cidrs;
             tokio::time::sleep(Duration::from_millis(100)).await;
         }
-    });
+        });
+    }
 
     EasyTier(Some(EasyTierHolder { instance, runtime }))
 }
diff --git a/src/lib.rs b/src/lib.rs
index c05a357..dce38cd 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -138,8 +138,8 @@ extern "system" fn JNI_OnLoad(vm: JavaVM, _: *mut c_void) -> jint {
             of!["start0", "(Ljava/lang/String;I)I", jni_start],
             of!["getState0", "()Ljava/lang/String;", jni_get_state],
             of!["setWaiting0", "()V", jni_set_waiting],
-            of!["setScanning0", "(Ljava/lang/String;Ljava/lang/String;)V", jni_set_scanning],
-            of!["setGuesting0", "(Ljava/lang/String;Ljava/lang/String;)Z", jni_set_guesting],
+            of!["setScanning0", "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V", jni_set_scanning],
+            of!["setGuesting0", "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)Z", jni_set_guesting],
             of!["verifyRoomCode0", "(Ljava/lang/String;)I", jni_verify_room_code],
             of!["getMetadata0", "()Ljava/lang/String;", jni_get_metadata],
             of!["prepareExportLogs0", "()J", jni_prepare_export_logs],
@@ -276,20 +276,20 @@ extern "system" fn jni_set_waiting<'l>(jenv: JNIEnv<'l>, _: JClass<'l>) {
     }
 }
 
-extern "system" fn jni_set_scanning<'l>(jenv: JNIEnv<'l>, _: JClass<'l>, room: JString<'l>, player: JString<'l>) {
+extern "system" fn jni_set_scanning<'l>(jenv: JNIEnv<'l>, _: JClass<'l>, room: JString<'l>, player: JString<'l>, nodes: JString<'l>) {
     try_jvm! { |jenv|
         let room = parse_jstring(&jenv, &room);
         let player = parse_jstring(&jenv, &player);
-        controller::set_scanning(room, player, vec![]);
+        controller::set_scanning(room, player, parse_nodes(&jenv, &nodes));
     }
 }
 
-extern "system" fn jni_set_guesting<'l>(jenv: JNIEnv<'l>, _: JClass<'l>, room: JString<'l>, player: JString<'l>) -> jboolean {
+extern "system" fn jni_set_guesting<'l>(jenv: JNIEnv<'l>, _: JClass<'l>, room: JString<'l>, player: JString<'l>, nodes: JString<'l>) -> jboolean {
     try_jvm! { |jenv|
         let room = parse_jstring(&jenv, &room).expect("'room' must not be NULL.");
         let player = parse_jstring(&jenv, &player);
 
-        if let Some(room) = Room::from(&room) && controller::set_guesting(room, player, vec![]) {
+        if let Some(room) = Room::from(&room) && controller::set_guesting(room, player, parse_nodes(&jenv, &nodes)) {
             JNI_TRUE
         } else {
             JNI_FALSE
@@ -358,4 +358,10 @@ fn parse_jstring<'l>(env: &JNIEnv<'l>, value: &JString<'l>) -> Option<String> {
         // SAFETY: value is a Java String Object
         unsafe { Some(env.get_string_unchecked(value).unwrap().into()) }
     }
-}
\ No newline at end of file
+}
+
+fn parse_nodes<'l>(env: &JNIEnv<'l>, value: &JString<'l>) -> Vec<String> {
+    parse_jstring(env, value)
+        .map(|nodes| nodes.split('\0').filter(|node| !node.is_empty()).map(str::to_owned).collect())
+        .unwrap_or_default()
+}
